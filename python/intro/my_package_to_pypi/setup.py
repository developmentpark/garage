from setuptools import setup

setup(name='stats_gaussian_binomial',
      version='0.1',
      description='Gaussian & Binomial distributions',
      packages=['stats_gaussian_binomial'],
      author= 'developmentpark',
      author_email='jnyupanquip@gmail.com',
      zip_safe=False)